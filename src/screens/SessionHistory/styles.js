import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
  },
  cardWrapper: {
    paddingTop: 20,
    paddingBottom: 20,
    paddingHorizontal:20,
    gap: 15,
  },
});
